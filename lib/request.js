'use strict';

const mime = require('mime'),
      fs = require('fs'),
      views = require('./views')(),
      legal = new RegExp('^' + (opts.dir || process.cwd()));

module.exports = function(rqe, res){

};
